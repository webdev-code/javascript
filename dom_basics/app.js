//window object means the tab we are currently at
// window object (browser api) provides us access to the browsers methods and properties
//console.dir provides all the properties and methods of an object
//HTML elements are stored in document object as nodes
// document objects is used to manipulate html elements (nodes in document object)
// document object has properties and methods to select the HTML elements and make changes to them

console.log(window);
console.dir(window);
console.log(document);
console.dir(document);
